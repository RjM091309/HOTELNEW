<div class="row checkin-notifier-page">
    <div class="col-xl-12 col-md-12 col-12">
        <div class="dashboard-card checkin-notifier-card">
            <div class="dashboard-card-header">
                <div class="checkin-notifier-header">
                    <h4 class="mb-0">Check-in Notifier</h4>
                    <button type="button" class="btn notify-btn" id="checkinNotifyBtn" disabled>
                        <i class="fa fa-bell"></i> NOTIFY
                    </button>
                    <div class="filter-btn-group">
                        <button type="button" class="filter-btn active" data-filter="all">All Upcoming</button>
                        <button type="button" class="filter-btn" data-filter="1day">1 Day Before</button>
                        <button type="button" class="filter-btn" data-filter="3day">3 Days Before</button>
                        <button type="button" class="filter-btn" data-filter="7day">1 Week Before</button>
                    </div>
                </div>
            </div>
            <div class="dashboard-card-body checkin-notifier-body">
                <div class="checkin-notifier-scroll-box">
                    <div class="table-scrollable checkin-notifier-table-wrap">
                    <table id="checkin_notifier_tbl" class="table table-hover table-bordered full-width">
                        <thead>
                            <tr>
                                <th class="notifier-select-col text-center"></th>
                                <th>#</th>
                                <th>GUEST NAME</th>
                                <th>CONTACT</th>
                                <th>ROOM</th>
                                <th>CHECK-IN</th>
                                <th>CHECK-OUT</th>
                                <th>PAYMENT STATUS</th>
                                <th>CHANNEL</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/booking/check_in_notifier_data.js?v=6"></script>

<style>
.checkin-notifier-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    flex-wrap: wrap;
    gap: 12px;
}

.notify-btn {
    background-color: #6f9c40;
    border: 1px solid #6f9c40;
    color: #fff;
    padding: 6px 18px;
    border-radius: 4px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.4px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.notify-btn:hover:not(:disabled) {
    background-color: #5d8636;
    border-color: #5d8636;
    color: #fff;
}

.notify-btn:disabled {
    opacity: 0.45;
    cursor: not-allowed;
}

.filter-btn-group {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.filter-btn {
    background-color: #2a3135;
    border: 1px solid #3B3B3B;
    color: #e0e0e0;
    padding: 6px 14px;
    border-radius: 4px;
    font-size: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.filter-btn:hover,
.filter-btn.active {
    background-color: #007bff;
    border-color: #007bff;
    color: #fff;
}

.payment-badge {
    display: inline-block;
    min-width: 88px;
    padding: 4px 10px;
    border-radius: 999px;
    font-size: 11px;
    font-weight: 600;
    text-align: center;
    text-transform: uppercase;
}

.payment-badge-paid {
    background-color: rgba(67, 160, 71, 0.2);
    color: #81c784;
}

.payment-badge-partial {
    background-color: rgba(0, 229, 255, 0.15);
    color: #4dd0e1;
}

.payment-badge-none {
    background-color: rgba(229, 57, 53, 0.2);
    color: #ff8a80;
}

.notifier-select-col {
    width: 36px;
    min-width: 36px;
}

#checkin_notifier_tbl_wrapper .notifier-row-check,
#checkin_notifier_tbl_wrapper #notifierSelectAll {
    width: 16px;
    height: 16px;
    cursor: pointer;
    accent-color: #6f9c40;
}

.checkin-notifier-page {
    overflow: hidden;
}

.checkin-notifier-page > [class*="col-"] {
    height: 100%;
}

.checkin-notifier-card {
    height: 100%;
    display: flex;
    flex-direction: column;
    min-height: 0;
    overflow: hidden;
}

.checkin-notifier-body {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 0;
    overflow: hidden;
    padding: 16px 20px 20px;
}

.checkin-notifier-scroll-box {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    background: linear-gradient(145deg, #2a3135, #1f2528);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
    overflow: hidden;
}

.checkin-notifier-table-wrap {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    margin: 0 !important;
    overflow: hidden !important;
}

.checkin-notifier-table-wrap > .table {
    margin-bottom: 0;
}

#checkin_notifier_tbl_wrapper {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 0;
    height: 100%;
    padding: 12px 12px 8px;
}

#checkin_notifier_tbl_wrapper .dataTables_filter {
    margin-bottom: 10px;
}

#checkin_notifier_tbl_wrapper .dataTables_filter label {
    color: #bdbdbd;
    margin-bottom: 0;
}

#checkin_notifier_tbl_wrapper .dataTables_filter input {
    background: #1f2528;
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: #e0e0e0;
    border-radius: 6px;
    margin-left: 8px;
}

#checkin_notifier_tbl_wrapper .dataTables_scroll {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    overflow: hidden;
    background: rgba(0, 0, 0, 0.12);
}

#checkin_notifier_tbl_wrapper .dataTables_scrollHead {
    flex: 0 0 auto;
}

#checkin_notifier_tbl_wrapper .dataTables_scrollBody {
    flex: 1 1 auto;
    min-height: 0 !important;
    max-height: none !important;
    overflow-y: auto !important;
    overscroll-behavior: contain;
}

#checkin_notifier_tbl_wrapper .dataTables_scrollBody::-webkit-scrollbar {
    width: 10px;
}

#checkin_notifier_tbl_wrapper .dataTables_scrollBody::-webkit-scrollbar-thumb {
    background-color: #6f9c40;
    border-radius: 6px;
}

#checkin_notifier_tbl_wrapper .dataTables_scrollBody::-webkit-scrollbar-track {
    background-color: rgba(255, 255, 255, 0.08);
}

#checkin_notifier_tbl_wrapper .dataTables_scrollBody {
    scrollbar-color: #6f9c40 rgba(255, 255, 255, 0.08);
    scrollbar-width: thin;
}

#checkin_notifier_tbl_wrapper .dataTables_info {
    flex: 0 0 auto;
    padding-top: 10px;
    color: #bdbdbd;
}

#checkin_notifier_tbl_wrapper .dataTables_paginate {
    display: none !important;
}

#checkin_notifier_tbl_wrapper .dataTables_length {
    display: none !important;
}

/* Light theme - this whole page skips the site's [data-theme="white"] split
   (scroll box, search input, filter buttons, payment badges are all
   hardcoded dark above), so none of it adapted when the rest of the UI went
   light. Filter buttons + table get the same #2196f3/#1565c0 blue accent
   used across /booking and /calendar/room-checker; payment badges reuse the
   same green/orange/red as /booking's own PAID/PARTIAL/UNPAID labels so the
   whole booking section reads as one system instead of drifting per page. */
[data-theme="white"] .notify-btn {
    background-color: #2196f3;
    border-color: #2196f3;
}
[data-theme="white"] .notify-btn:hover:not(:disabled) {
    background-color: #1e88e5;
    border-color: #1e88e5;
}

[data-theme="white"] .checkin-notifier-scroll-box {
    background: #ffffff;
    border: 1px solid #e5e7eb;
    box-shadow: none;
}

[data-theme="white"] .filter-btn {
    background-color: rgba(33, 150, 243, 0.08);
    border: 1px solid rgba(33, 150, 243, 0.25);
    color: #1565c0;
}

[data-theme="white"] .filter-btn:hover,
[data-theme="white"] .filter-btn.active {
    background-color: #2196f3;
    border-color: #2196f3;
    color: #ffffff;
}

[data-theme="white"] .payment-badge-paid {
    background-color: rgba(76, 175, 80, 0.12);
    color: #2e7d32;
}
[data-theme="white"] .payment-badge-partial {
    background-color: rgba(255, 152, 0, 0.12);
    color: #e65100;
}
[data-theme="white"] .payment-badge-none {
    background-color: rgba(244, 67, 54, 0.12);
    color: #c62828;
}

[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_filter label,
[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_info {
    color: #4b5563;
}

[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_filter input {
    background: #ffffff;
    border: 1px solid #d1d5db;
    color: #111827;
}

[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_scroll {
    border: 1px solid #e5e7eb;
    background: #ffffff;
}

[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_scrollBody::-webkit-scrollbar-track {
    background-color: #eef2f7;
}

/* table.css/theme-color.css's base table rules (gray thead, dark row hover)
   are !important and unscoped by page - need !important + an ID here to win.
   This table uses DataTables scrollX/scrollY, which CLONES the <thead> into
   a separate .dataTables_scrollHead table for the visible sticky header -
   the original #checkin_notifier_tbl's own thead stays in the DOM but is
   visually collapsed inside .dataTables_scrollBody, so it has to be targeted
   separately (by the never-cloned wrapper id) or the visible header misses it. */
[data-theme="white"] #checkin_notifier_tbl thead th,
[data-theme="white"] #checkin_notifier_tbl_wrapper .dataTables_scrollHead thead th {
    background: rgba(33, 150, 243, 0.08) !important;
    color: #1565c0 !important;
    border-color: rgba(33, 150, 243, 0.25) !important;
}
[data-theme="white"] #checkin_notifier_tbl tbody td,
[data-theme="white"] #checkin_notifier_tbl tbody th {
    border-color: rgba(33, 150, 243, 0.15) !important;
}
[data-theme="white"] #checkin_notifier_tbl.table-hover tbody tr:hover,
[data-theme="white"] #checkin_notifier_tbl.table-hover tbody tr:hover td {
    background: rgba(33, 150, 243, 0.06) !important;
    color: #111827 !important;
}
</style>
