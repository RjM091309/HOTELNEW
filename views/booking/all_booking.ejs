<!-- start page content -->
<div class="row">
    <div class="col-xl-12 col-md-12 col-12">
        <div class="dashboard-card">
            <div class="dashboard-card-header">
                <div style="display: flex; align-items: center; justify-content: space-between; width: 100%;">
                    <div class="tab-bar">
                        <a class="tab-item" href="/booking">Single</a>
                        <a class="tab-item" href="/booking/group">Group</a>
                        <a class="tab-item" href="/booking/agency">Agency</a>
                        <a class="tab-item is-active" href="/booking/all">All</a>
                    </div>

                    <div style="flex: 1; display: flex; justify-content: center; margin: 0 20px;">
                        <div class="filter-btn-group">
                            <button type="button" class="filter-btn active" data-filter="all">All</button>
                            <button type="button" class="filter-btn" data-filter="today">Today</button>
                            <button type="button" class="filter-btn" data-filter="last3days">Last 3 Days</button>
                            <button type="button" class="filter-btn" data-filter="thisWeek">This Week</button>
                            <button type="button" class="filter-btn" data-filter="thisMonth">This Month</button>
                        </div>
                    </div>

                    <div style="flex: 0 0 auto; display: flex; gap: 10px;">
                        <button type="button" class="btn btn-success" data-bs-toggle="modal"
                        data-bs-target="#modal-addbooking"><i class="fa fa-plus"></i> Add
                            Booking
                        </button>
                        <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                        data-bs-target="#modal-add-group-booking"><i class="fa fa-users"></i> Add
                           Group Booking
                        </button>
                    </div>
                </div>
            </div>
            <div class="dashboard-card-body">
                <div class="table-scrollable">
                    <table id="booking_tbl" class="table table-hover table-bordered full-width">
                        <thead>
                        <tr>
                            <th style="display:none"></th>
                            <th>#</th>
                            <th>GUEST NAME</th>
                            <th>ROOM NUMBER</th>
                            <th>CONFIRMATION NUMBER</th>
                            <th>CHECK IN</th>
                            <th>CHECK OUT</th>
                            <th>TOTAL PAYMENT</th>
                            <th>BALANCE</th>
                            <th>BOOKING CHANNEL</th>
                            <th>PAYMENT STATUS</th>
                            <th>BOOKING STATUS</th>
                            <th>ACTION</th>
                        </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../booking/modal/billing') %>
<%- include('../booking/modal/status') %>
<%- include('../booking/modal/cancel_booking') %>
<%- include('../booking/modal/edit_booking') %>
<%- include('../booking/modal/add_booking') %>
<%- include('../booking/modal/add_group_booking') %>

                    <!-- Voucher Details Modal -->
                    <div class="modal fade" id="modal-voucher-details" tabindex="-1" role="dialog" aria-labelledby="modal-voucher-details-label" aria-hidden="true" data-bs-backdrop="static">
                        <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="modal-voucher-details-label">
                                        <i class="fa fa-file-text-o"></i> Voucher Details
                                    </h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="table-responsive">
                                        <table class="table table-bordered table-striped">
                                            <tbody>
                                                <tr>
                                                    <th class="bg-light" style="width: 40%;">SOURCE / GUEST NAME</th>
                                                    <td id="voucher-guest-name">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">RESERVATION DATE</th>
                                                    <td id="voucher-reservation-date">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">ARRIVAL DATE</th>
                                                    <td id="voucher-checkin">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">DEPARTURE DATE</th>
                                                    <td id="voucher-checkout">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">CHECK-IN STATUS</th>
                                                    <td id="voucher-checkin-status">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">CHECK-OUT STATUS</th>
                                                    <td id="voucher-checkout-status">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">LENGTH OF STAY</th>
                                                    <td id="voucher-length-stay">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">ROOM TYPE</th>
                                                    <td id="voucher-room-type">-</td>
                                                </tr>
                                                <tr>
                                                    <th class="bg-light">REMARKS</th>
                                                    <td id="voucher-remarks">-</td>
                                                </tr>
                                                <tr id="reservation-fee-row" style="display: none;">
                                                    <th class="bg-light">RESERVATION FEE</th>
                                                    <td id="voucher-reservation-fee">-</td>
                                                </tr>
                                                <tr id="discount-amount-row" style="display: none;">
                                                    <th class="bg-light">DISCOUNT AMOUNT</th>
                                                    <td id="voucher-discount-amount">-</td>
                                                </tr>
                                                <tr id="grand-total-row">
                                                    <th class="bg-dark">GRAND TOTAL</th>
                                                    <td id="voucher-grand-total">-</td>
                                                </tr>
                                                <tr class="bg-dark">
                                                    <th class="bg-dark text-white">TOTAL BALANCE</th>
                                                    <td id="voucher-total-balance" class="fw-bold text-white">-</td>
                                                </tr>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                    <button type="button" class="btn btn-info" onclick="downloadVoucherFromModal()">
                                        <i class="fa fa-download"></i> Download Voucher
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

<script src="/js/booking/booking_data.js"></script>
<script src="/js/dashboard/room-menu_data.js"></script>
<script>
    window.openRemarksModal = openRemarksModal;
    // Open existing group modal + fetch details
    function openGroupFromAll(groupId){
        // Reuse groupBookingModal from group page if present; if not, create minimal modal
        let modalEl = document.getElementById('groupBookingModal');
        if (!modalEl){
            const tmpl = document.createElement('div');
            tmpl.innerHTML = `
            <div class="modal fade" id="groupBookingModal" tabindex="-1" aria-hidden="true">
                <div class="modal-dialog modal-lg"><div class="modal-content">
                    <div class="modal-header"><h5 class="modal-title">Individual Booking Details</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <table class="table table-bordered"><thead>
                            <tr><th style="width: 120px;">Guest Name</th><th>Room Number</th><th>Check-In Date</th><th>Check-Out Date</th><th style="width: 120px;">Status</th><th>Payment</th><th>Services Availed</th></tr>
                        </thead><tbody></tbody></table>
                        <div class="mt-3">
                            <h6 class="mb-2">Summary</h6>
                            <table class="table table-sm table-striped table-bordered align-middle" id="groupBookingSummary">
                                <thead class="table-dark">
                                    <tr>
                                        <th class="text-start" style="width: 75%">Description</th>
                                        <th class="text-end" style="width: 25%">Amount</th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                                <tfoot>
                                    <tr class="table-dark">
                                        <td class="text-start"><b>Grand Total</b></td>
                                        <td id="groupBookingSummaryGrand" class="text-end"><b>0.00</b></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>
                </div></div></div>`;
            document.body.appendChild(tmpl.firstElementChild);
            modalEl = document.getElementById('groupBookingModal');
        }

        // Fetch details
        $.get(`/booking/group_booking_details/${groupId}`, function(resp){
            const tbody = modalEl.querySelector('tbody');
            tbody.innerHTML = '';
            (resp.bookingDetails || []).forEach(row => {
                const tr = document.createElement('tr');
                const fmt = (d) => d ? new Date(d).toLocaleDateString('en-US', { month:'short', day:'numeric', year:'numeric' }) : '-';
                tr.innerHTML = `<td>${row.CUSTOMER_NAME ?? '-'}</td>
                                <td>${row.ROOM_NUMBER ?? '-'}</td>
                                <td>${fmt(row.CHECK_IN_DATE)}</td>
                                <td>${fmt(row.CHECK_OUT_DATE)}</td>
                                <td>${row.BOOKING_STATUS ?? '-'}</td>
                                <td>${row.TOTAL_COST != null ? parseFloat(row.TOTAL_COST).toLocaleString('en-US', { minimumFractionDigits: 2 }) : '-'}</td>
                                <td>${row.SERVICES_AVAILED ?? '-'}</td>`;
                tbody.appendChild(tr);
            });

            // Render summary if available
            const summary = resp.summary || null;
            const sumBody = modalEl.querySelector('#groupBookingSummary tbody');
            const sumGrand = modalEl.querySelector('#groupBookingSummaryGrand');
            if (sumBody) {
                sumBody.innerHTML = '';
            }
            if (summary && sumBody && sumGrand){
                const fmtAmt = (n) => parseFloat(n || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
                const nRoom = parseFloat(summary.roomTotal || 0);
                const nServices = parseFloat(summary.servicesTotal || 0);
                const nExt = parseFloat(summary.extensionsTotal || 0);
                const nDisc = parseFloat(summary.discount || 0);
                const nRes = parseFloat(summary.reservationFee || 0);
                const nGrand = parseFloat(summary.grandTotal || 0);

                const rows = [];
                rows.push(`<tr><td class="text-start">Rooms Total</td><td class="text-end">${fmtAmt(nRoom)}</td></tr>`);
                if (nServices > 0) rows.push(`<tr><td class="text-start">Services Total</td><td class="text-end">${fmtAmt(nServices)}</td></tr>`);
                if (nExt > 0) rows.push(`<tr><td class="text-start">Extensions Total</td><td class="text-end">${fmtAmt(nExt)}</td></tr>`);
                if (nDisc > 0) rows.push(`<tr><td class="text-start">Less: Discount</td><td class="text-end text-danger">-${fmtAmt(nDisc)}</td></tr>`);
                if (nRes > 0) rows.push(`<tr><td class="text-start">Less: Reservation Fee</td><td class="text-end text-danger">-${fmtAmt(nRes)}</td></tr>`);

                sumBody.innerHTML = rows.join('');
                sumGrand.innerHTML = `<b>${fmtAmt(nGrand)}</b>`;
            }
            const bsModal = new bootstrap.Modal(modalEl);
            bsModal.show();
        }).fail(() => {
            Swal.fire({ icon: 'error', title: 'Failed', text: 'Unable to load group details.' });
        });
    }
</script>

<style>
#modal-voucher-details .form-label { font-size: 12px; }
#modal-voucher-details .btn-close { filter: invert(1) !important; }
#modal-voucher-details .btn-close:hover { filter: invert(0.8) !important; }
#modal-voucher-details .modal-content { background-color: #2a3135; color: #e0e0e0; border-radius: 6px; box-shadow: 0 0 10px rgba(59,59,59, 0.2); border: 1px solid #3B3B3B; }
#modal-voucher-details .modal-header { background-color: #0c2a42; color: #ffffff; border-bottom: 1px solid #0c2a42; }
#modal-voucher-details .modal-title { color: #ffffff; }
#modal-voucher-details .table { background-color: #2a3135; color: #e0e0e0; }
#modal-voucher-details .table th { background-color: #41474c !important; color: #e0e0e0 !important; border-color: #3B3B3B !important; }
#modal-voucher-details .table td { background-color: #41474c !important; color: #e0e0e0 !important; border-color: #3B3B3B !important; }
#modal-voucher-details .bg-light { background-color: #41474c !important; color: #ffffff !important; }
#modal-voucher-details .modal-footer { background-color: #2a3135; border-top: 1px solid #3B3B3B; }
</style>

